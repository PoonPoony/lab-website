---
import Base from '../../layouts/Base.astro';
import HomeView from '../../views/HomeView.astro';
import { useSite } from '../../i18n/utils';
const lang = 'en' as const;
const s = useSite(lang);
---
<Base lang={lang} title={s.labName} current="home">
  <HomeView lang={lang} />
</Base>
