---
import Base from '../../layouts/Base.astro';
import MembersView from '../../views/MembersView.astro';
import { useT } from '../../i18n/utils';
const lang = 'en' as const;
const t = useT(lang);
---
<Base lang={lang} title={t('members.title')} current="members">
  <MembersView lang={lang} />
</Base>
